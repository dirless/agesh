name: agesh
version: 0.0.1

description: AGE-encrypted terminal access client/server

authors:
  - "Dirless <hello@dirless.io>"

license: MIT

crystal: ">= 1.20.0"

dependencies:
  age-crystal:
    path: ../age-crystal

targets:
  agesh:
    main: src/cli/client.cr
  agesh-server:
    main: src/cli/server.cr

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
